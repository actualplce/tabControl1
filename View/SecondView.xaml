<UserControl x:Class="tabControl1.SecondView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tabControl1"
        mc:Ignorable="d"
        Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="7*"/>
            <ColumnDefinition  Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="view2" Background="Aquamarine" Grid.Column="0"/>


    <StackPanel Grid.Column="1" Background="Azure">
            <Button Content="Plot" 
                    Command="{Binding PlotBtnCommand}"
                    Margin="10" />
        </StackPanel>
        
    </Grid>
</UserControl>
