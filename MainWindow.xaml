<Window x:Class="tabControl1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:tabControl1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.Resources>
            <SharedViewModel:TemplateSelector x:Key="TemplateSelector"
                                FirstTypeTemplate="FirstView"
                                SecondTypeTemplate="SecondView"/>

        <DataTemplate x:Key="FirstView">
            <TextBlock Text="1st View"/>
            <!--   <DockPanel>
                <Image DockPanel.Dock="Right"
                    Stretch="None"
                    Source="{Binding Path=Face}" />
                <UniformGrid
                    Rows="2"
                    VerticalAlignment="Center"
                    Margin="10">
                    <TextBlock
                        TextAlignment="Center"
                        Text="{Binding Path=Name}" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=BirthDate.Month}" />
                        <TextBlock Text="/" />
                        <TextBlock Text="{Binding Path=BirthDate.Day}" />
                        <TextBlock Text="/" />
                        <TextBlock Text="{Binding Path=BirthDate.Year}" />
                    </StackPanel>
                </UniformGrid>
            </DockPanel>-->
        </DataTemplate>
        <DataTemplate x:Key="SecondView">
            <TextBlock Text="2nd View"/>
            <!--
            <DockPanel>
                <Image DockPanel.Dock="Left"
                    Stretch="None"
                    Source="{Binding Path=Face}" />
                <UniformGrid
                    Rows="2"
                    VerticalAlignment="Center"
                    Margin="10">
                    <TextBlock
                        TextAlignment="Center"
                        Text="{Binding Path=Name}" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=BirthDate.Month}" />
                        <TextBlock Text="/" />
                        <TextBlock Text="{Binding Path=BirthDate.Day}" />
                        <TextBlock Text="/" />
                        <TextBlock Text="{Binding Path=BirthDate.Year}" />
                    </StackPanel>
                </UniformGrid>
            </DockPanel>-->
        </DataTemplate>
        <Style
            TargetType="{x:Type Button}">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment"   Value="Center" />
            <Setter Property="Margin"              Value="12"     />
        </Style>
    </Grid.Resources>
  
        <ScrollViewer>
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <Button ContentTemplateSelector="{StaticResource EmployeeDataTemplateSelectorKey}">
                    <Button.Content>
                 
                    </Button.Content>
                </Button>
                <Button ContentTemplateSelector="{StaticResource EmployeeDataTemplateSelectorKey}">
                    <Button.Content>
                        <local:Employee
                            Name="Edgar"
                            BirthDate="1965/02/02"
                            Face="IMAGE/Edgar.png"
                            LeftHanded="True" />
                    </Button.Content>
                </Button>
                <Button ContentTemplateSelector="{StaticResource EmployeeDataTemplateSelectorKey}">
                    <Button.Content>
                        <local:Employee
                            Name="Sally"
                            BirthDate="1980/07/12"
                            Face="IMAGE/Sally.png"
                            LeftHanded="True" />
                    </Button.Content>
                </Button>
                <Button ContentTemplateSelector="{StaticResource EmployeeDataTemplateSelectorKey}">
                    <Button.Content>
                        <local:Employee
                            Name="Jim"
                            BirthDate="1975/06/15"
                            Face="IMAGE/Jim.png"
                            LeftHanded="False" />
                    </Button.Content>
                </Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>


